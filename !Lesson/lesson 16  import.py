import math   # сам ипорт
dir(math)    # показывает какие вариации одержит
help(math.sqrt)   # показывает что значит одна из вариации
from math import sqrt, ....  #  это если нужно дернуть конкретную функцию а не весь модуль
from math imoprt sqrt as fwefw # импортируем модуль и присваем свое имя ему.
from math import *   # импорт всех функции из math